![[Pasted image 20260118214516.png]]
модель задает вопросы к данным -> хранит эти вопросы в вершинах -> разбивает на подвыборки и формирует листья с предсказаниями

***Какие вопросы нужно задавать?***
![[Pasted image 20260118221457.png]]
перебираем признаки которые дадут нормальное разбиение по 2 ветвям
***Функционал качества дерева***
![[Pasted image 20260118221744.png]]
- X_m множество обьектов попавших в вершину на данном шаге
- X_r,l множества попадающие в правое и левое подмножества
- H(X) - показывает хаос, непорядок -> задача минимизации ошибок и максимизации прироста информации Q
- Нужно нормировать, пример:
- ![[Pasted image 20260118222950.png]]
**Критерий энтропии Шеннона**:
вероятность быть этим классом * на логарифм_2 этой вероятностиЮ в идеале должна быть 0 (когда все обьекты к 1 классу). Энтропия позволяет количество неопределнности, хаоса в данных 

**Критерий энтропии Джини**:
тоже самое
![[Pasted image 20260118223639.png]]
![[Pasted image 20260118223832.png]]
![[Pasted image 20260118224048.png]]

чем Q больше тем лучше
![[Pasted image 20260118224113.png]]
меньше значит хуже, вопрос плохой
Итог:
![[Pasted image 20260118224133.png]]
На деле у деревьев могут быть сотни тысячи вопросов
Находим лучшие вопросы через уникальные значения либо среднее, но не берем где все обьекты на одной ветви
С помощью перебора постоянно считая энтропию разделяем данные пока не настигнем критерия останова
**Весь процесс**
1. ap_hi <= 125. 6 относятся к классу 0, 4 к 1
![[Pasted image 20260118224518.png]]


# Билет 8. Деревья решений. Критерии Entropy и Gini

## 1. Что такое дерево решений

**Дерево решений** — это модель, которая:

- задаёт **вопросы к данным**;
    
- хранит эти вопросы **в вершинах дерева**;
    
- на каждом шаге **разбивает выборку на подвыборки**;
    
- в листьях хранит **предсказание класса** (или распределение классов).
    

Каждый путь от корня к листу — это **последовательность условий**.

---

## 2. Какого вида вопросы задаёт дерево

В дереве решений используются **простые бинарные вопросы** вида:

`признак_j ≤ t ?`

Примеры:

- `ap_hi ≤ 120`
    
- `age ≤ 45`
    
- `income > 50000`
    

Ответ всегда:

- **да → левая ветвь**
    
- **нет → правая ветвь**
    

---

## 3. Главный вопрос при построении дерева

### ❓ Какие вопросы нужно задавать, чтобы дерево было хорошим?

Идея такая:

> Нужно выбрать такой вопрос,  
> который **лучше всего разделяет классы**.

То есть после разбиения:

- в подмножествах должно быть **меньше хаоса**,
    
- объекты одного класса должны группироваться вместе.
    

---

## 4. Обозначения (как в лекции)

- XmX_mXm​ — множество объектов, попавших в текущую вершину
    
- XlX_lXl​, XrX_rXr​ — объекты, попавшие в левую и правую ветвь
    
- H(X)H(X)H(X) — мера **неопределённости (хаоса)** в множестве
    

Задача дерева:

> уменьшать неопределённость после каждого разбиения.

---

## 5. Функционал качества разбиения

Качество вопроса оценивается через **прирост информации**:

`Q(X_m, j, t) = H(X_m)              − |X_l| / |X_m| · H(X_l)              − |X_r| / |X_m| · H(X_r)`

### Смысл формулы:

- берём **хаос до разбиения**;
    
- вычитаем **взвешенный хаос после разбиения**;
    
- чем больше Q, тем **лучше вопрос**.
    

⚠️ Важно:  
нормировка через `|X_l| / |X_m|` и `|X_r| / |X_m|` нужна,  
чтобы учитывать **размеры подвыборок**.

---

## 6. Что такое H(X): критерии хаоса

### 6.1 Энтропия Шеннона (Entropy)

`H(X) = − Σ p_k · log₂(p_k)`

Где:

- pkp_kpk​ — доля объектов класса k.
    

**Интерпретация:**

- H = 0 → все объекты одного класса (идеально);
    
- H максимальна → классы перемешаны.
    

Энтропия показывает **степень неопределённости**.

---

### 6.2 Индекс Джини (Gini)

`Gini(X) = 1 − Σ p_k²`

**Интерпретация:**

- Gini = 0 → чистый узел;
    
- чем больше Gini → тем сильнее перемешаны классы.
    

По смыслу:

> Gini и Entropy делают одно и то же —  
> измеряют “хаос” в данных.

---

## 7. Как дерево выбирает лучший вопрос

Алгоритм в вершине:

1. Перебираем **все признаки**.
    
2. Для каждого признака перебираем возможные пороги:
    
    - уникальные значения;
        
    - либо средние между отсортированными значениями.
        
3. Отбрасываем разбиения, где:
    
    - все объекты уходят в одну ветвь.
        
4. Для каждого допустимого разбиения считаем **Q**.
    
5. Выбираем вопрос с **максимальным Q**.
    

👉 **Чем больше Q — тем лучше вопрос.**  
👉 Малый Q → вопрос плохой.

---

## 8. Пример разбиения (как на слайде)

`ap_hi ≤ 125`

В текущей вершине:

- 6 объектов класса 0
    
- 4 объекта класса 1
    

После разбиения:

- в одной ветви почти чистый класс 0,
    
- в другой — почти чистый класс 1.
    

👉 хаос резко уменьшился → Q большое → хороший вопрос.

---

## 9. Как строится дерево целиком

**Весь процесс построения дерева:**

1. В корне есть вся обучающая выборка.
    
2. Находим лучший вопрос по максимуму Q.
    
3. Делим данные на две подвыборки.
    
4. Повторяем процесс рекурсивно для каждой вершины.
    
5. Останавливаемся, когда:
    
    - узел стал чистым;
        
    - мало объектов;
        
    - достигнута максимальная глубина;
        
    - дальнейшее разбиение не даёт выигрыша.
        

---

## 10. Важные практические замечания

- В реальных деревьях могут быть **сотни и тысячи вопросов**.
    
- Без ограничений дерево легко **переобучается**.
    
- Поэтому используют критерии останова или обрезку (pruning).
    

---

## 11. Итог (коротко, но правильно)

> Дерево решений строится путём жадного выбора бинарных вопросов, которые максимизируют уменьшение неопределённости в данных. Для измерения хаоса используются критерии энтропии Шеннона или индекса Джини, а качество разбиения оценивается через прирост информации.

---

## 12. Фраза для экзамена (золотая)

> На каждом шаге дерево выбирает такой признак и порог, которые дают максимальный прирост информации, то есть минимизируют взвешенную неопределённость в дочерних узлах.