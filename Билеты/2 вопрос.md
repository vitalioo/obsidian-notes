KNN и оптимизации. Расстояния. FRiS**

### 🔹 Алгоритм KNN

**K Nearest Neighbors** — метрический алгоритм классификации или регрессии.

Идея:

- объекты одного класса находятся близко,
    
- объектов разных классов — далеко (гипотеза компактности).
    

**Предсказание:**

1. считается расстояние от нового объекта до всех объектов обучающей выборки;
    
2. выбираются K ближайших соседей;
    
3. ответ определяется голосованием (обычным или взвешенным).
    

**Важно:**

- обучающая выборка = модель;
    
- обучения в классическом смысле нет;
    
- K — гиперпараметр.
    

---

### 🔹 Метрики расстояния

- **Евклидово** — обычное расстояние «по прямой», чувствительно к выбросам.
    
- **Манхэттенское** — сумма модулей разностей, более устойчиво к шуму.
    
- **Косинусное** — сравнивает направление векторов, а не длину  
    (часто используется для текстов и эмбеддингов).
    

Все метрические алгоритмы **чувствительны к масштабу признаков**, поэтому признаки необходимо нормировать или стандартизировать.

---

### 🔹 Проблемы KNN

1. **Медленное предсказание** — нужно сравнивать с каждым объектом (O(N)).
    
2. **Зависимость от масштаба признаков**.
    
3. **Чувствительность к шуму**.
    
4. **Проклятие размерности**.
    

---

### 🔹 Оптимизации KNN

Для ускорения поиска соседей используются структуры данных:

- **KD-tree** — разбиение пространства по координатам (эффективно в малой размерности).
    
- **Ball tree** — разбиение по шарам, лучше работает при большей размерности.
    
- **HNSW** — графовый приближённый поиск ближайших соседей, очень быстрый.
    

Эти методы уменьшают число сравнений за счёт отбрасывания целых областей пространства.

---

### 🔹 FRiS–Stolp

**FRiS–Stolp** — метод отбора эталонных объектов для метрических алгоритмов.

Идея:

- хороший объект должен быть:
    
    - близок к объектам своего класса,
        
    - далёк от объектов других классов.
        

**FRiS-функция** оценивает, насколько объект является хорошим представителем своего класса.

Использование:

- удаление шума,
    
- уменьшение обучающей выборки,
    
- повышение стабильности KNN.
    

Важно:

- FRiS **не ускоряет поиск напрямую**,
    
- он улучшает качество данных.