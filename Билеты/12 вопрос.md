12. Нейронные сети. Граф вычислений. Метод обратного распространения ошибки. Chain-rule

> Мы используем сигмоиду вместо step, потому что нейросети обучаются через производные, а у step-функции производной нет.

![[Pasted image 20260119224925.png]]
**Delta выхода = насколько выход реагирует на изменение входа**
Производная говорит:

> **насколько сильно выход нейрона меняется, если чуть изменить веса**

![[Pasted image 20260119232236.png]]
> **Backprop нужен, чтобы каждый скрытый нейрон понял,  
> насколько он виноват в финальной ошибке.**


# Chain Rule (правило цепочки) — кратко и по делу

## Идея
**Chain rule** отвечает на вопрос:  
> как изменение одного параметра влияет на результат, если между ними несколько шагов (функций).

Если одна величина зависит от второй, а та — от третьей,  
то влияние считается **через произведение производных**.

---

## Общая форма

Если:
- \( y = f(u) \)
- \( u = g(x) \)

то:
\[
\frac{dy}{dx}
=
\frac{dy}{du}
\cdot
\frac{du}{dx}
\]

---

## Пример с тремя шагами (как в нейросети)

Пусть:
- \( z = w \cdot x \)
- \( a = \sigma(z) \)
- \( L = (a - y)^2 \)

Хотим:
\[
\frac{\partial L}{\partial w}
\]

По chain rule:
\[
\frac{\partial L}{\partial w}
=
\frac{\partial L}{\partial a}
\cdot
\frac{\partial a}{\partial z}
\cdot
\frac{\partial z}{\partial w}
\]

📌 **умножаем производные вдоль пути зависимости**

---

## Формы записи (важно знать)

### 1. Композиция функций
\[
L(w) = f(g(h(w)))
\Rightarrow
\frac{dL}{dw} = f'(g(h(w))) \cdot g'(h(w)) \cdot h'(w)
\]

---

### 2. В терминах графа вычислений
\[
\frac{\partial \text{output}}{\partial \text{parameter}}
=
\prod \text{локальных производных по пути}
\]

---

### 3. Для нейросетей (backprop)
\[
\frac{\partial L}{\partial w}
=
\text{error}
\times
\sigma'(z)
\times
\text{input}
\]

---

## Смысл (по-человечески)

- результат зависит от параметра **не напрямую**
- chain rule говорит:
> «насколько параметр влияет на результат, проходя через все промежуточные шаги»

---

## Зачем в нейросетях
- нейросеть = **цепочка функций**
- backprop = **многократное применение chain rule**
- без chain rule нельзя посчитать, как менять веса

---

## Одна фраза для экзамена

> Правило цепочки позволяет вычислять производные сложной функции как произведение производных её составных частей и лежит в основе алгоритма обратного распространения ошибки.

